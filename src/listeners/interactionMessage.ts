// import { BaseCommandInteraction, Client, Interaction, Message, MessageActionRow, MessageButton } from "discord.js";
// import { Commands } from "../commands";

// export default (client: Client): void => {
//     client.on("message", async (message: Message) => {
//         if (message.author.client) return;
//         if(message.content.toLowerCase() == 'test'){
//            const row = new MessageActionRow()
//     .addComponents(
//         new MessageButton()
//             .setCustomId('primary')
//             .setLabel('Primary')
//             .setStyle('PRIMARY'),
//     );

// await interaction.reply({ content: 'Pong!', components: [row] });
//         }
//     });
    
// };

// const handleMessage = async (client: Client, interaction: BaseCommandInteraction): Promise<void> => {
    
   
    

// };